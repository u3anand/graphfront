
class Hamiltonian {
    constructor(G) {
        this.cycleCount = this.hamiltonianCycleCount(G);
        this.cycles = this.hamiltonianCycles(G);
    }

    hamiltonianCycleCount = (G) => {

    }

    hamiltonianCycles = (G) => {
        
    }
}

export default Hamiltonian
